<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/whitePrimary">

    <TextView
        android:id="@+id/suggestion_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/msv_select_filter_left_margin"
        android:paddingRight="@dimen/msv_select_filter_left_margin"
        android:textColor="#727272"
        android:layout_centerVertical="true"
        android:focusable="false"
        android:textSize="16sp" />

    <ImageView
        android:id="@+id/suggestion_icon"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:paddingLeft="@dimen/msv_select_filter_icon_padding"
        android:paddingRight="@dimen/msv_select_filter_icon_padding"
        android:focusable="false"
        android:src="@drawable/ic_close_grey600_36dp" />
</RelativeLayout>